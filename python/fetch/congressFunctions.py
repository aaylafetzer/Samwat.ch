import requests
import feedparser


def getHouseFloor(url):
    feed = feedparser.parse(url)


def getSenateFloor(url):
    feed = feedparser.parse(url)


def getPresentedToPresident(url):
    feed = feedparser.parse(url)

